# pal-tool web dashboard — environment variables
# Copy this file to .env and fill in your values

# Middleware API key (required for mw-health)
# Wrap the value in single quotes if it contains special characters ($, !, etc.)
MW_API_KEY='your_api_key_here'

# Port for the local web server (optional, defaults to 3000)
# PORT=3000
