# pal-tool web dashboard — environment variables
# Copy this file to .env and fill in your values

# Middleware credentials (required for mw-health)
# TOTP is entered in the UI each time you run
OCC_USER=your_user@example.com
OCC_PASSWORD=your_password_here

# Port for the local web server (optional, defaults to 3000)
# PORT=3000
